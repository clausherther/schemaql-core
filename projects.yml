projects:
  tpch:
    connection: tpch-snowflake
    schema:
      TPCH:
      - ODS
      - WH
  omaze:
    connection: omaze-snowflake
    schema:
      # fivetran:
      # - shopify
      DW:
      - customer
      - shop
      - marketing
      - web
  omaze-test:
    connection: omaze-snowflake
    schema:
      # fivetran:
      # - shopify
      DW:
      - shop
  # - connection: omaze-snowflake
  #   schema:
  #     - shop
  #     - customer